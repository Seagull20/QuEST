# Common Benchmark Helpers

Shared support code for:

- CLI parsing and runtime metadata
- TSV append helpers
- deterministic RNG for random layered circuits
- backend/runtime validation shared by all benchmarks
